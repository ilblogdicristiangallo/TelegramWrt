#!/bin/sh

echo "📡 WAN IP & Gateway:"
echo ""

# IP pubblico via servizio esterno
PUBLIC_IP=$(curl -s https://ifconfig.me)

# Gateway locale
GATEWAY=$(ip route | grep default | awk '{print $3}')

echo "• Public IP: $PUBLIC_IP"
echo "• Default Gateway: $GATEWAY"
