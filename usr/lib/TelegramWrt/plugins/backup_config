#!/bin/sh

echo "⚙️ Creating config backup:"
echo ""

BACKUP_DIR="/root/config_backup"
mkdir -p "$BACKUP_DIR"

for file in network wireless firewall system; do
  cp "/etc/config/$file" "$BACKUP_DIR/$file"
done

echo "✅ Backup completed in $BACKUP_DIR"
