#!/bin/sh

echo "📡 DNS Status:"
echo ""

echo "• DNS configured via UCI:"
uci show network | grep -i dns | sed 's/^/  - /'

echo ""
echo "• DNS currently used (/etc/resolv.conf):"
grep -E '^nameserver' /etc/resolv.conf | sed 's/^/  - /'
